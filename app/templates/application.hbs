<FormSelect
  @hint="Select your country of residence"
  @label="Country"
  @options={{array "Mexico" "Canada" "USA"}}
  @selected={{this.country}}
  @onChange={{action (mut this.country)}} as |country|
>
  {{country}}
</FormSelect>

{{outlet}}